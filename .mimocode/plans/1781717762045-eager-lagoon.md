# Plan: Update AGENTS.md

## Current state
The existing `AGENTS.md` at root is mostly accurate but is missing key information accumulated during this session.

## Changes needed

### 1. Add Android Manifest permissions note
The app needs Bluetooth permissions in `AndroidManifest.xml`. An agent would miss this when working on the Bluetooth/BandBurg features.

### 2. Add NDK version requirement
`ndkVersion = "30.0.14904198"` is set in `androidApp/build.gradle.kts`. Agents need to know this.

### 3. Add Visual Studio Build Tools requirement
Rust cross-compilation requires MSVC `link.exe`. MSYS2's `link.exe` in PATH will break cargo builds. Agent must install VS Build Tools with "C++ Desktop development" workload.

### 4. Add rust-android-gradle plugin info
The `net.mullvad.rust-android` plugin (v0.10.1) is used. The `cargo` block config lives in `androidApp/build.gradle.kts`.

### 5. Add expect/actual pattern notes
`BandBurgManager` and `BluetoothScanner` use expect/actual. Desktop/WasmJS have stubs. Agent modifying shared code must update all platforms.

### 6. Add ktlint compose modifier order rule
`modifier-clickable-order` requires `.clickable` after `.background(shape)`. Agents will hit this repeatedly with spotless.

### 7. Add kotlinx-serialization dependency
`libs.kotlinx.serialization.json` is in commonMain. `SavedDevice` uses `@Serializable`.

## File to modify
- `D:\Android\Project\Miband\AGENTS.md`

## Verification
Read the updated file to confirm all entries are accurate and compact.
